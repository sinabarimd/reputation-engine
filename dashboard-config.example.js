// Dashboard configuration template
// Copy this to dashboard-config.js and fill in your values
// dashboard-config.js is gitignored and should never be committed
const CONFIG = {
  N8N_BASE_URL: 'https://your-n8n-instance.example.com',
  PW_HASH: '',  // SHA-256 hash of your dashboard password
  GH_TOKEN: '', // GitHub personal access token (for repo traffic stats)
  GH_REPO: '',  // GitHub repo in owner/repo format
};
